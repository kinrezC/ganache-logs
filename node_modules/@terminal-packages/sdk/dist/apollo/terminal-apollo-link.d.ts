import { ApolloLink, FetchResult, NextLink, Observable, Operation } from 'apollo-link';
import { ITerminalApolloLinkOptions } from './models/iterminal-apollo-link-options';
export declare class TerminalApolloLink extends ApolloLink {
    private _options;
    private _apiService;
    constructor(_options: ITerminalApolloLinkOptions);
    /**
     * Watches the GraphQL request made by the ApolloClient
     * @param operation The current GraphQL operation
     * @param forward The next connected ApolloLink context
     */
    request(operation: Operation, forward: NextLink): Observable<FetchResult> | null;
}
