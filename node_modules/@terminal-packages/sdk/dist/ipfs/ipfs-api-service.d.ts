import { ApiService } from '../shared/api/api-service';
import { IPostRequestOptions } from '../shared/api/requests';
import { ITerminalIpfsOptions } from './models/iterminal-ipfs-options';
export declare class IpfsApiService {
    private _apiService;
    private _apiUrl;
    private _path;
    private _options;
    constructor(_apiService: ApiService, _apiUrl: string, _path: string, _options: ITerminalIpfsOptions);
    /**
     * Get request on the api url and search
     * @param search The search url
     */
    get<T>(search: string): Promise<T>;
    /**
     * Post request on the api url and path
     * @param endpoint The request endpoint
     * @param body The request body
     * @param options The request options
     */
    post<T>(endpoint: string, body: any, options?: IPostRequestOptions): Promise<T>;
    /**
     * Extract the method from the path and the endpoint
     * @param endpoint The endpoint
     */
    private method;
    /**
     * Get parameters from the query search url
     * @param search The search string
     */
    private parametersFromQuerySearch;
}
