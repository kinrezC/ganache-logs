import { ApiService } from '../../../shared/api/api-service';
import { ITerminalIpfsOptions } from '../../models/iterminal-ipfs-options';
import { IBlockPutRequest, IBlockPutResponse, IBlockStatResponse } from './models';
export declare class Block {
    private _ipfsApiService;
    constructor(apiService: ApiService, apiUrl: string, options: ITerminalIpfsOptions);
    /**
     * Get a raw IPFS block.
     * @param multihash The base58 multihash of an existing block to get
     */
    get(multihash: string): Promise<string>;
    /**
     * Print information of a raw IPFS block.
     * @param multihash The base58 multihash of an existing block to stat
     */
    stat(multihash: string): Promise<IBlockStatResponse>;
    /**
     * Store input as an IPFS block
     * @param blockPutRequest The block put request
     */
    put(blockPutRequest: IBlockPutRequest): Promise<IBlockPutResponse>;
}
