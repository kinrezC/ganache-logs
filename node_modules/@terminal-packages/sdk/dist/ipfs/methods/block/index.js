"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../../../shared/utils");
const ipfs_api_service_1 = require("../../ipfs-api-service");
class Block {
    constructor(apiService, apiUrl, options) {
        this._ipfsApiService = new ipfs_api_service_1.IpfsApiService(apiService, apiUrl, 'block/', options);
    }
    /**
     * Get a raw IPFS block.
     * @param multihash The base58 multihash of an existing block to get
     */
    get(multihash) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this._ipfsApiService.get(`get?arg=${multihash}`);
        });
    }
    /**
     * Print information of a raw IPFS block.
     * @param multihash The base58 multihash of an existing block to stat
     */
    stat(multihash) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this._ipfsApiService.get(`stat?arg=${multihash}`);
        });
    }
    /**
     * Store input as an IPFS block
     * @param blockPutRequest The block put request
     */
    put(blockPutRequest) {
        return __awaiter(this, void 0, void 0, function* () {
            const formData = yield utils_1.Utils.getFormData();
            formData.append('file', blockPutRequest.fileStringOrBlob);
            let requestPath = '';
            if (blockPutRequest.format) {
                requestPath += `&format=${blockPutRequest.format}`;
            }
            if (blockPutRequest.mhtype) {
                requestPath += `&mhtype=${blockPutRequest.mhtype}`;
            }
            if (blockPutRequest.mhlen !== undefined) {
                requestPath += `&mhlen=${blockPutRequest.mhlen}`;
            }
            if (requestPath.length > 0) {
                requestPath = `?${requestPath.substring(1)}`;
            }
            return yield this._ipfsApiService.post(`put${requestPath}`, formData);
        });
    }
}
exports.Block = Block;
