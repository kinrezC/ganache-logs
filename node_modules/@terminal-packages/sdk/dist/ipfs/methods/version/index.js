"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const ipfs_api_service_1 = require("../../ipfs-api-service");
class Version {
    constructor(apiService, apiUrl, options) {
        this._ipfsApiService = new ipfs_api_service_1.IpfsApiService(apiService, apiUrl, 'version', options);
    }
    /**
     * Show IPFS version information
     * @param versionRequest The version request payload
     */
    get(versionRequest) {
        return __awaiter(this, void 0, void 0, function* () {
            let requestPath = '';
            if (versionRequest) {
                if (versionRequest.number !== undefined) {
                    requestPath += `&number=${versionRequest.number.toString()}`;
                }
                if (versionRequest.commit !== undefined) {
                    requestPath += `&commit=${versionRequest.commit.toString()}`;
                }
                if (versionRequest.repo !== undefined) {
                    requestPath += `&repo=${versionRequest.repo.toString()}`;
                }
                if (versionRequest.all !== undefined) {
                    requestPath += `&all=${versionRequest.all.toString()}`;
                }
                if (requestPath.length > 0) {
                    requestPath = `?${requestPath.substring(1)}`;
                }
            }
            return yield this._ipfsApiService.get(requestPath);
        });
    }
}
exports.Version = Version;
