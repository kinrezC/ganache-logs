import { EnvironmentTypes } from '../../shared/enums/environment-types';
import { SourceType } from '../../web3/providers/enums';
export interface ITerminalIpfsOptions {
    uri: string;
    apiKey: string;
    projectId: string;
    environment: EnvironmentTypes;
    source: SourceType | string;
    headers?: Array<{
        name: string;
        value: string;
    }>;
}
