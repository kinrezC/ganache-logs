import { ITerminalWSClientProviderOptions } from './models/iterminal-wsclient-provider-options';
import { WSClient } from '@0x/mesh-rpc-client';
export declare class TerminalWSClientProvider extends WSClient {
    private _wsProvider;
    private _originalWsProvider;
    private terminalBaseProviderInstance;
    constructor(_options: ITerminalWSClientProviderOptions);
    /**
     * overrideSendPayload will ensure that we can intercept the JSON RPC call
     * it should enable TerminalBaseProvider to work as intended and call the real WS provider
     */
    private overrideSendPayload;
}
