import { AbstractWeb3Module } from 'web3-core';
import { AbstractMethod } from 'web3-core-method';
import { ISendAsyncPayload } from './models/isend-async-payload';
export interface ITerminalBaseProvider {
    connected: boolean;
    host: string | undefined;
    send(method: string, parameters: any[]): Promise<any>;
    sendAsync(payload: ISendAsyncPayload, callback: (error: Error | null, result: any) => void): Promise<void>;
    sendBatch(methods: AbstractMethod[], moduleInstance: AbstractWeb3Module): Promise<any[]>;
}
