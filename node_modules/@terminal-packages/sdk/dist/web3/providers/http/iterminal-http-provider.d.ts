import { ITerminalBaseProvider } from '../base/iterminal-base-provider';
export interface ITerminalHttpProvider extends ITerminalBaseProvider {
    connected: boolean;
    supportsSubscriptions(): boolean;
    disconnect(): boolean;
}
